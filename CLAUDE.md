# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

- `npm start` / `ng serve` — dev server at `http://localhost:4200/`, live reload
- `npm run build` / `ng build` — production build, output in `dist/`
- `npm run watch` / `ng build --watch --configuration development` — dev build with watch mode
- `npm test` / `ng test` — run unit tests via Karma/Jasmine (watches by default; runs in Chrome)
- `ng generate component <name>` — scaffold a new component (see `ng generate --help` for other schematics)

There is no e2e test setup and no lint script configured yet.

## Architecture

This is an Angular 20 project using the modern standalone-component style (no `NgModule`s):

- `src/main.ts` bootstraps `App` using `appConfig`.
- `src/app/app.config.ts` is the central provider registration point (`ApplicationConfig`). It currently wires up global error listeners, **zoneless change detection** (`provideZonelessChangeDetection()` — this app does not use `zone.js`, so change detection must be triggered via signals/`OnPush`-compatible patterns rather than relying on zone patching), and the router.
- `src/app/app.routes.ts` holds the route table: `/` (overview), `/pokemon/:id`, `/move/:id`, all lazy via `loadComponent`.
- `src/app/app.ts` is the root standalone component (selector `app-root`); just renders `AppHeader` + `<router-outlet />`, no state of its own.

Code style: Prettier is configured in `package.json` (100 print width, single quotes, Angular parser for `.html` files). Component schematics default to `style: css` + `changeDetection: OnPush` (see `angular.json`), so `ng generate component` already matches convention.

## Current status (2026-08-17)

Phases 0–5 are done. Phase 6 (Move detail page / `MoveDetailAccordion`) is next.

- **Phase 0/1** — Tailwind v4 (`.postcssrc.json` + `src/styles.css`, `@theme` block), Swiper installed (not wired into any component yet), folder skeleton, `environment.ts`/`environment.development.ts` (`pokeApiBaseUrl`), models for `Pokemon`/`PokemonSpecies`/`Move`/`EvolutionChain` in `core/models/` (snake_case, mirror the raw PokeAPI JSON — no DTO mapping layer). `PokemonCacheService` (3 separate signal `Map`s for Pokemon/Species/EvolutionChain), `PokemonApiService`, `MoveApiService`.
- **Phase 2** — Routes for `/`, `/pokemon/:id`, `/move/:id` (lazy `loadComponent`) in `app.routes.ts`; `withComponentInputBinding()` is enabled so route params arrive as `input()` signals on page components. `AppHeader` (persistent) hosts `GlobalSearchBar`, which is still just a disabled placeholder input — its real logic is Phase 4's job.
- **Phase 3** — Overview page is a working vertical slice: `PokemonGrid`/`PokemonCard`/`LoadMoreTrigger`/`GenerationSelector`/`SearchFilterBar`, wired into `OverviewPage`. Default browsing paginates the national dex via `/pokemon?limit&offset`; selecting a generation hits `/generation/{id}`, selecting a type hits `/type/{name}` (a real fetch, not a client-only filter over what's loaded); selecting both intersects the two result sets client-side. Sprite images for grid tiles are built directly from the numeric ID (`core/utils/pokemon.util.ts` → `getPokemonSpriteUrl`, pointed at `raw.githubusercontent.com/PokeAPI/sprites`) instead of fetching each Pokémon individually — this is a deliberate deviation from "PokemonApiService is the sole access point," justified purely to avoid N+1 fetches for a grid; revisit if that tradeoff stops making sense.
- **Phase 4** — `SearchIndexService` (`core/services/search-index.service.ts`) builds a one-time EN/DE/ID index: `GET /pokemon-species?limit=20000` for the full list, then `GET /pokemon-species/{id}` per entry (concurrency-capped at 20 via `mergeMap`) to pull `names` for `en`/`de`. Built as an `rxResource` field initializer gated by a `triggered` signal (`params: () => triggered() ? true : undefined`, same idle-gating pattern as `getGeneration`/`getType`) — **not** eagerly loaded on app boot; `ensureLoaded()` is called lazily from `GlobalSearchBar` (on focus/input), from `OverviewPage` (once `nameQuery` is non-empty, or once the language is switched to `de` — see below), and is idempotent. Pure matching logic lives in `core/utils/search.util.ts` (`filterSearchIndex` — numeric query matches by ID, otherwise substring on EN or DE name) so both consumers share it. `GlobalSearchBar` shows a live dropdown (top 8 matches, "Suchindex wird geladen…" while the index builds) and navigates to `/pokemon/:id` on selection. `SearchFilterBar`'s in-page name filter (consumed in `OverviewPage`) unions three checks so it never regresses while the index is still loading: instant English-slug substring match, instant numeric-ID match against the already-known `GridItem.id` (no index needed), and DE-name/ID match from the search index once it's loaded.
- **Phase 4 follow-up (same day, user feedback after the initial pass)** — two additions beyond the original roadmap wording:
  - **EN/DE language toggle** — `LanguageService` (`core/services/language.service.ts`, a single `signal<'en'|'de'>('en')`), toggle buttons in `AppHeader`. Switching to `de` triggers `SearchIndexService.ensureLoaded()` (user picked this explicitly over a lighter per-page-only fetch, accepting the one-time ~20-40s full-index cost so search AND grid names share one cache from then on) and `OverviewPage` remaps `visibleItems()`'s `GridItem.name` to the DE name once available (falls back to the EN slug until the index resolves — no flash of empty grid). `GlobalSearchBar`'s dropdown picks EN/DE name for display the same way; the underlying search match itself always checks both languages regardless of the active display language. `PokemonCard`/`PokemonGrid` stay language-agnostic — they just render whatever `name` they're given; only the container (`OverviewPage`) touches `LanguageService`/`SearchIndexService`, per the "only container/page components reach into services" rule.
  - **Dual-type filter** — `SearchFilterBar` now has two type `<select>`s (`selectedType1`/`selectedType2`); each one's options exclude whatever the other has selected, so the same type can't be picked twice. `OverviewPage.filteredPool` intersects every *active* filter (generation, type 1, type 2) generically via a `reduce` over "types that have a value selected" rather than hardcoded two-way logic, so it stays correct however many of the three are active. Verified against the real API with a throwaway spec that intersection of `/type/fire` and `/type/flying` real API results contains `charizard`.
  - Verified against the real API with throwaway specs (written, run, deleted): EN/DE name mapping for a known Pokémon + `filterSearchIndex` matching logic, and the fire∩flying dual-type intersection. `ng build` and `ng test --watch=false` both pass (had to add `provideHttpClient()`/`provideHttpClientTesting()` to `app.spec.ts`, `app-header.spec.ts`, `global-search-bar.spec.ts` since they now transitively inject `SearchIndexService`).
  - **UI copy also follows the toggle** (user feedback, same day) — `core/utils/i18n.util.ts` (`pick(language, en, de)`) is a tiny helper, no i18n framework added. Used for input placeholders, the "Filter(s)" button, `GlobalSearchBar`'s "Suchindex wird geladen…"/"Building search index…" and "Keine Treffer"/"No matches", `SearchFilterBar`'s type-select default options, and a static `TYPE_LABELS_DE` map (18 entries) so the 18 Pokémon type names in the type selects show German labels too (no API call needed, these are fixed). `GenerationSelector` and `SearchFilterBar` inject `LanguageService` directly despite being presentational — treated as an exception to the "only containers touch services" rule since it's pure UI/locale state, not data-fetching (same category as Angular's `LOCALE_ID`, not `PokemonApiService`). The `TYPE_LABELS_DE` map and its `typeLabel(type, language)` helper now live in `core/constants/pokemon-type.constants.ts` (moved out of `search-filter-bar.ts` in Phase 5 once `PokemonHeader`/`TypeEffectiveness` needed the same labels).
- **Phase 5 (in progress)** — Pokémon detail page, "core" slice done:
  - **Fixed a latent non-reactivity bug** in `PokemonApiService.getPokemon`/`getPokemonSpecies`/`getEvolutionChain`: they used to take a plain dereferenced value (`getPokemon(idOrName: string | number)`) instead of a reactive callback, so if the same routed component were reused across param changes (e.g. `/pokemon/1` → `/pokemon/2` without a remount) the resource would never refetch. Nothing consumed these three methods before Phase 5, so this was latent, not yet user-facing. Changed to the same `params: () => T | undefined` + idle-gating convention as `getType`/`getGeneration`/`getAbility`.
  - **`Ability` model + `getAbility`** added to `PokemonApiService`/`PokemonCacheService` (mirrors the existing 3-map cache pattern) — not in the original datenfluss doc's service list, added because `AbilitiesList` needs `/ability/{name}` for the accordion effect text. Verified against the real API: ability `effect_entries` *does* cover `de` (not just `en`), e.g. `static`'s entries include `['fr','de','en']`.
  - **`TypeDetail` model extended** with `damage_relations` (PokeAPI's `/type/{name}` already returns it, just wasn't modeled yet). `core/utils/type-effectiveness.util.ts` → `computeTypeEffectiveness(relations: TypeRelations[])` multiplies per-type multipliers across the Pokémon's 1-2 types (double/half/no damage_from), bucketing into weak/resistant/immune — matches real in-game dual-type math since 0 (no_damage) always wins under multiplication. Verified against the real API: electric (single-type) and fire+flying/Charizard (dual-type, including the "resists fairy too" case) both match the known in-game chart.
  - **`AbilitiesList` reaches into `PokemonApiService` directly** (another exception to "only containers touch services", alongside `LanguageService` in Phase 4) — justified because it owns a single-open accordion (`expandedName` signal) that both gates and gets identified by the same lazy `getAbility()` fetch; hoisting that state to `PokemonDetailPage` would mean passing expand-state and fetched-detail both ways for no real benefit. `TypeEffectiveness` stays purely presentational by contrast — `PokemonDetailPage` (the container) fetches both of the Pokémon's `/type/{name}` and passes the already-computed weak/resistant/immune arrays down, since that's a fixed 1-2 calls tied to already-known data, not a dynamic per-item concern.
  - `PokemonDetailPage` computes the localized display name from `PokemonSpecies.names` (same `find(language) ?? find('en')` fallback pattern as everywhere else) and prefers `sprites.other['official-artwork'].front_default`, falling back to `getPokemonSpriteUrl(id)` if artwork art is missing.
  - Verified against the real API with throwaway specs (written, run, deleted): `computeTypeEffectiveness` matchups above, and pikachu's `/pokemon` + `/pokemon-species` response shape matches what `PokemonHeader`/`BaseStats`/`AbilitiesList` read. `ng build` and `ng test --watch=false` (15 specs) both pass.
- **Phase 5 (done)** — `EvolutionChain` and `MovesetTable`/`MoveTooltip` completed the detail page:
  - **Same latent non-reactivity bug, found again in `MoveApiService.getMove`** — fixed the same way as `PokemonApiService` above (`params: () => string | number | undefined` + idle-gating), needed since `MoveTooltip` and (later) the move detail page both call it reactively off a hover/route signal.
  - **`core/utils/evolution.util.ts`** (`flattenEvolutionChain`, `evolutionRequirementLabel`) flattens the chain's tree via DFS into slide order — handles branching chains (e.g. Eevee's 8 evolutions) by just visiting every node, not just linear chains. Requirement text covers min_level, item, min_happiness (→ "Friendship"/"Freundschaft"), and trade as named cases, with a generic slug-capitalize fallback for the rest (day/night, held-item-at-location combos etc. aren't specially labeled — acceptable per-node fallback, not exhaustive). Verified against the real API: bulbasaur's chain flattens to exactly `[bulbasaur, ivysaur, venusaur]` with min_level 16/32.
  - **`EvolutionChain` is the first real Swiper usage** in the project — uses the **Swiper Element** (web component) variant per the official Angular guidance at swiperjs.com/element, not the plain JS class API (an earlier pass used the class API + `afterNextRender`; the user explicitly asked for Element instead, since that's what the docs recommend for framework integration). `import { register } from 'swiper/element/bundle'; register();` called once at module scope (top-level side effect, not inside the class — custom element registration is global and must happen exactly once per app). Template uses the real `<swiper-container>`/`<swiper-slide>` custom elements directly with kebab-case attributes (`slides-per-view="auto" space-between="16"`) instead of manual init/destroy/`.update()` JS calls — the element manages its own lifecycle. Requires `schemas: [CUSTOM_ELEMENTS_SCHEMA]` on the component so Angular's template compiler allows the unknown-to-Angular tag names. No separate `swiper/css` import needed — Element ships its own styles via shadow DOM. The `element/bundle` subpath pulls in all Swiper modules (~185KB, up from ~69KB for a bare core-only build), which is an acceptable tradeoff here since it's still fully lazy via `@defer` and never touches the main bundle.
  - **`core/utils/moveset.util.ts`** (`buildMovesetRows`) flattens `Pokemon.moves[].version_group_details[]` into rows deduped by `(move, method, level)` — deliberately does **not** pick "the current game"/version group (nothing in the roadmap specifies one, and the app has no cross-page "selected generation" state to reuse), so a move learned at the same level/method across many games collapses to one row, while genuinely different level requirements across games each get their own row. Verified against the real API: pikachu's `thunder-shock` collapses correctly and level-up rows sort before machine/egg/tutor rows.
  - **Both `EvolutionChain` and `MovesetTable` are wrapped in `@defer (on viewport)`** in `pokemon-detail-page.html`, per the datenfluss doc — confirmed this actually code-splits (Swiper only ships in the separate `evolution-chain` chunk, not the main bundle).
  - **Two more "container-ish exceptions"** (same category as `AbilitiesList`/`LanguageService` in earlier phases, documented here rather than repeated per-case): `MovesetTable` and `MoveTooltip` reach into `MoveApiService` directly — `MovesetTable` owns hover position + the hovered move's name (the natural owner of that transient UI state), and `MoveTooltip` owns its own single `getMove()` resource gated by its `moveName` input (same single-resource-gated-by-active-item pattern as `AbilitiesList`'s accordion, reused here for hover instead of expand). `EvolutionChain` fetches its own chain data (rather than the page container fetching and passing it down) specifically *because* it's the `@defer`-ed component — deferring the bundle but having the container eagerly fetch the data would defeat half the point of `@defer`.
  - Evolution-chain and grid-tile German names both reuse `SearchIndexService.entries()` opportunistically (no forced `ensureLoaded()` call from `EvolutionChain` — chains are only 2-8 species, not worth the full ~1300-species index load; falls back to the EN slug if the index isn't already loaded from search/grid use).
  - Verified against the real API with throwaway specs (written, run, deleted) for both utils above. `ng build` and `ng test --watch=false` (18 specs) both pass.
- **Phase 5 follow-up (same day, user feedback after Swiper-Element fix)**:
  - **Type badges now use real colors** — `core/constants/pokemon-type-colors.constants.ts` (`typeColor`/`typeTextColor`, the standard 18-type hex palette, white text except on light backgrounds like electric/ice/ground/fairy/normal/steel which get dark text). Applied via `[style.background-color]`/`[style.color]` bindings (not Tailwind classes, since the 18 colors are per-type dynamic data, not static utility classes) in `PokemonHeader`, `TypeEffectiveness`, `MoveTooltip`'s type badge, and as a small colored dot per row in `MovesetTable` (a full colored badge per row read as too heavy against 30-90 rows — a dot gives type-at-a-glance without the visual weight). `SearchFilterBar`'s native `<select>`/`<option>` type dropdowns were deliberately left uncolored — per-`<option>` background styling is unreliable cross-browser and it's a filter control, not a type display.
  - **`MovesetTable` now localizes move names, not just German type/method labels** — `MoveApiService.getMoves(params: () => string[] | undefined)` bulk-fetches (concurrency-capped at 15, same `mergeMap` pattern as `SearchIndexService`) every distinct move in the current moveset once, populating the same cache `getMove()` reads from. Deliberately scoped per-Pokémon (~30-90 moves) rather than a global all-~900-moves index like `SearchIndexService` — nothing else needs a global move-name index yet, so building one preemptively would be premature. Rows fall back to the EN slug until the bulk fetch resolves. Verified against the real API that `/move/tackle` includes a `de` name and that `getMoves()` called from a real injection context returns both requested moves with correct types.
  - **`MovesetTable`'s method filter now defaults to `'level-up'`** (was "all methods") — user-requested default, since level-up is the most commonly-wanted view of a moveset.
  - **Evolution slider fixes** — `w-full` on `<swiper-container>` (was unconstrained-width, wasn't filling its row), added `centered-slides="true"` and `navigation="true"` attributes (prev/next arrows are Swiper Element's built-in shadow-DOM buttons, no extra markup needed), and a CSS rule scaling the active slide 1.5× (`.evolution-slide.swiper-slide-active { transform: scale(1.5) }` in `evolution-chain.css`) — confirmed via `node_modules/swiper/types/options.d.ts` that `slideActiveClass` defaults to exactly `'swiper-slide-active'` and is applied directly to the `<swiper-slide>` host element (not buried in its shadow DOM), so a normal Angular-scoped CSS selector can target it.
  - **Sprite CDN outage — root cause and fix (same day)**: user reported "images stopped loading" across the app. **Not a regression from any of the above changes** — `curl -I` against `raw.githubusercontent.com/PokeAPI/sprites/...` returned `429 Too Many Requests` consistently (verified repeatedly), while the same path via `cdn.jsdelivr.net/gh/PokeAPI/sprites/...` (jsDelivr's GitHub-raw mirror, meant for hotlinking/production, unlike `raw.githubusercontent.com`) returned `200`. `getPokemonSpriteUrl()` in `core/utils/pokemon.util.ts` now points at the jsDelivr URL. **Also fixed**: `PokemonDetailPage.spriteUrl` used to prefer `pokemon.sprites.other['official-artwork'].front_default` — that field is a *full URL returned directly by the PokeAPI response*, which itself points at `raw.githubusercontent.com`, so it was equally broken and wouldn't have been fixed by changing `getPokemonSpriteUrl` alone. Now `PokemonDetailPage` always uses `getPokemonSpriteUrl(id)` (jsDelivr) and never touches `sprites.other`, which also happens to better match the product concept's "simple sprites, no shiny/high-end artwork focus". Spot-checked jsDelivr across several IDs (1, 25, 150, 493) — all `200`.
  - **Evolution slider now navigates as you slide** (user-requested) — `EvolutionChain` gained a `currentId` input (the Pokémon currently open on the detail page, passed from `PokemonDetailPage` as `[currentId]="pokemon.id"`), used to compute `initialSlideIndex` so the slider opens centered on the Pokémon you're already viewing, not always slide 0. Navigates to `/pokemon/{id}` for whatever slide ends up active, reading the real `node.id` from the flattened chain data (never index-derived) — verified against real, deliberately non-sequential chains (Eevee: 133/134/135/136/196/197/470/471/700) to rule out any accidental index-as-id assumption, plus the exact user-reported case (seel 86 → dewgong 87). `slide-to-clicked-slide="true"` unifies click and swipe into the same code path — clicking a slide centers it first, which fires the same slide-change event as swiping, so there's exactly one navigation trigger.
    - **Gotcha, found the hard way**: Swiper's `SwiperContainerEventMap` types (`node_modules/swiper/swiper-element.d.ts`) document event keys like `slidechangetransitionend` *without* the `swiper` prefix, but the actual dispatched DOM `CustomEvent` name has `eventsPrefix` (default `'swiper'`, see `node_modules/swiper/shared/swiper-core.mjs`) prepended — so the real event is `swiperslidechangetransitionend`, and binding `(slidechangetransitionend)` in the Angular template silently never fires (no error, just dead code — this shipped once already and only surfaced when the user tested it manually, since nothing in `ng build`/`ng test` catches an event name that's simply never dispatched). **If adding a new Swiper Element event listener, always prefix with `swiper` and verify against a real dispatched event, not just the `.d.ts` key name.** Confirmed the fix in a real-Chrome throwaway spec (mounted actual `<swiper-container>`/`<swiper-slide>` elements, called `.slideNext()`, asserted `swiperslidechangetransitionend` fires with `detail: [swiperInstance]` and `slidechangetransitionend` does not) — this is the first real interaction/event-driven behavior in the project verified end-to-end in a real browser rather than through data-layer specs alone, since no screenshot tool is available to check this kind of thing visually.
  - **Fixed the actual navigation UX** (same day, user tested and reported it "works now" for the event fix, then flagged two more symptoms): once the slider event fired correctly, the user reported the *rest of the page* still showed the old Pokémon after navigating, and the slider itself jumped back to the first stage instead of staying on the one just picked. **Root cause for both, traced into `resource.mjs` itself**: `PokemonDetailPage`'s whole template was gated behind `@if (isLoading())`, and `resource()`'s internal `linkedSignal` computation explicitly drops `stream` (and therefore `.value()`) back to `undefined` the instant the request/params change (`stream: previous.value.extRequest.request === extRequest.request ? previous.value.stream : undefined` — confirmed by reading the source, not guessed), *before* the new value has loaded. So every slide-triggered navigation briefly collapsed the entire page — including the mounted `EvolutionChain`/`MovesetTable` below the fold — back to a one-line "Lädt…", which both looked like the viewport jumping to the top (the content the user was scrolled to had literally been removed) and destroyed/recreated `EvolutionChain` (resetting the slider's position, since `initial-slide` is only read once at element creation). **Fix**: `PokemonDetailPage` now keeps `stickyPokemon`/`stickySpecies` signals that only ever get overwritten with a *defined* resource value (via two `effect()`s), and the template checks `@if (pokemon(); as pokemon)` *before* the loading/error branches — so the previously-loaded Pokémon stays fully rendered (stale-while-revalidate) until the new one is ready, and the "Lädt…"/error states now only apply to the true first load. This is a deliberate "look like the data just swaps in place" choice per the user's own description, with no loading indicator during a same-page navigation — verified with a throwaway spec using `HttpTestingController` that changes the `id` input mid-flight (before flushing the new response) and asserts `pokemon()` still holds the *previous* Pokémon's data at that exact moment, then updates once the new response resolves.

### Gotchas learned the hard way (read before touching resources/services again)

- **`rxResource()` on this Angular version (20.3.28) uses `params`/`stream`, not the older `request`/`loader` naming** — check `node_modules/@angular/core/rxjs-interop/index.d.ts` / `api.d.d.ts` directly if unsure, don't trust memory/older docs.
- **`params` returning `undefined` keeps a resource idle (no request); `null` does not** — it happily fetches `.../undefined`-shaped URLs otherwise. Optional filter params (e.g. `selectedGeneration`, `selectedType`) must be typed `T | undefined`, never `T | null`.
- **`PokemonApiService`/`MoveApiService` methods that return `rxResource()` must be called from a real injection context** (a component field initializer or constructor) — do not wrap them in `runInInjectionContext(someInjector, ...)` inside the service using the *service's own* injector. That was tried in Phase 1 and reverted in Phase 3: it silently binds the resource's lifecycle to the root injector instead of the calling component, so the resource is never torn down when the component is destroyed (a leak). Calling these methods as a component field initializer works correctly with no wrapper needed.
- **`LoadMoreTrigger`'s `IntersectionObserver` only fires on intersection *crossings*, not continuously** — gating `loadMore.emit()` only inside the observer callback caused a real, reproduced bug (see commit `a9c2aa2`): the sentinel could already be visible on mount, firing `loadMore()` while the first page was still loading, which raced ahead and silently dropped page 1. Fix: track intersection in a signal and re-evaluate via `effect()` on every `disabled()` change too, and make sure `disabled` accounts for the relevant resource's `isLoading()`, not just "no more data". Any future infinite-scroll/defer-loaded section (e.g. `EvolutionChain`, `MovesetTable`) should reuse `LoadMoreTrigger` as-is rather than re-implementing this.
- No screenshot/browser-automation tool is available in this environment — verification so far has relied on `ng build`, `ng test`, and temporary throwaway spec files that hit the **real** PokeAPI (written, run, then deleted — see git history for examples). Keep doing that for new data-layer work; ask the user to eyeball actual UI/UX in their own browser.
- **A `resource()`'s `.value()` resets to `undefined` the instant its `params`/request changes, even if the previous value is still perfectly valid to show** (confirmed by reading `resource.mjs`'s `linkedSignal` computation, not guessed). Gating an entire template behind `@if (resource.isLoading())` therefore un-mounts and rebuilds *everything* in that branch — including any deferred/stateful children like `EvolutionChain`'s Swiper instance — on every single param change, not just the first load. This bit us on `PokemonDetailPage`: navigating to a different Pokémon from the evolution slider blanked the whole page and reset the slider's position. **If a routed/param-driven page should feel like its data "swaps in place" rather than reloading, keep a sticky signal that only gets overwritten with *defined* resource values (via `effect()`), and check `@if (stickyValue(); as x)` before the loading/error branches** — see `PokemonDetailPage.stickyPokemon`/`stickySpecies` for the pattern. Reach for this any time a page can be re-navigated to a sibling of itself (same route, different param) without a full remount.
- **Swiper Element's documented event keys in `swiper-element.d.ts` omit the `eventsPrefix` default (`'swiper'`)** — the type says `slidechangetransitionend`, the actual dispatched `CustomEvent` is `swiperslidechangetransitionend`. Binding the undocumented-prefix name compiles fine and fails silently (never fires, no error). Verify any new Swiper Element event binding against a real dispatched event (e.g. a throwaway spec mounting a real `<swiper-container>` and listening for both names), not just the `.d.ts`.

The sections below summarize the planning docs in `claude md's/` (kept there as the detailed source of truth; not auto-loaded by Claude Code since they're outside the project root, hence mirrored here). They describe the *full* target design; check "Current status" above for what's actually built so far — everything from Phase 4 onward below is still just the plan.

## Target stack & conventions (see `claude md's/pokedex-techstack.md`)

- Standalone components only, no `NgModule`s; `ChangeDetectionStrategy.OnPush` on every component
- State via `signal()` / `computed()` / `effect()` — no NgRx, no RxJS-based store, no `BehaviorSubject` services
- Inputs/outputs via the `input()` / `output()` signal APIs instead of `@Input()`/`@Output()` decorators where possible
- Styling: Tailwind CSS v4 with `@theme` for design tokens (colors, spacing, breakpoints), utility classes directly in templates — no per-component SCSS files, no `tailwind.config.js` (not yet installed)
- Swiper.js for horizontal scroll/carousel UI (sprite gallery, evolution chain, move cards)

## Product concept (see `claude md's/pokedex-konzept.md`)

A team-planning Pokédex focused on the single-Pokémon detail view, not a 6-Pokémon team builder:

- Core: type-effectiveness overview (weaknesses/resistances/immunities), base stats as bar chart + numbers, abilities with effect descriptions
- Movesets: full learnable moveset per Pokémon, filterable by learn method (level-up/TM/egg), hover for quick info, click for a full move detail page (power, accuracy, PP, effect)
- Navigation: search/filter by name/type/generation, one generation/game selectable at a time, simple sprites (no shiny/high-end artwork focus), evolution chain shown as a classic tree
- Explicitly out of scope: a 6-Pokémon team composer/builder

## Component structure (see `claude md's/pokedex-components.md`)

Three pages: overview/search page, Pokémon detail page, move detail page.

**Global layout**
- `AppHeader` — persistent header on every page, hosts `GlobalSearchBar`
- `GlobalSearchBar` — app-wide search by English name, German name, or numeric Pokédex ID; live dropdown suggestions fed by `SearchIndexService`; a hit navigates straight to the Pokémon detail page

**Overview page**
- `PokemonGrid` — tiled grid of sprites
- `PokemonCard` — single grid tile
- `SearchFilterBar` — in-page filter bar (separate from the global search): search on top, collapsible filters (name EN/DE, type, generation/game) below; embeds `GenerationSelector`
- `GenerationSelector` — picks which generation/game's Pokémon are shown
- `LoadMoreTrigger` — `IntersectionObserver`-based infinite scroll / "load more" at the grid's end

**Pokémon detail page**
- `PokemonHeader` — large sprite + name/types
- `TypeEffectiveness` — three columns: weak / resistant / immune
- `BaseStats` — one bar + number per stat (HP, Attack, Defense, Sp. Atk, Sp. Def, Speed)
- `AbilitiesList` — abilities stacked vertically, effect text revealed accordion-style on expand
- `EvolutionChain` — Swiper slider through evolution stages
- `MovesetTable` — full learnable moveset; filter row with a learn-method dropdown (level-up / TM/VM / egg / etc.) plus a `type="number"` level filter (empty = inactive, first activation defaults to 1); rows show move left / requirement right (`justify-between`); list re-filters live as method/level change
- `MoveTooltip` — cursor-following hover tooltip with the short version of a move's info; click navigates to the move detail page

**Move detail page**
- `MoveDetailAccordion` — FAQ-style accordion, one expandable item per move, full details (power, accuracy, PP, effect) on expand; no sprites needed here

`AppHeader`/`GlobalSearchBar`, `GenerationSelector`, and `LoadMoreTrigger` were added after the initial concept pass — they weren't literal component names in `pokedex-konzept.md` but were needed to actually implement its search/generation/lazy-load requirements.

## Data flow & services (see `claude md's/pokedex-datenfluss.md`)

No NgRx, no manual RxJS `.subscribe()` management. State lives in services as signals; components consume it read-only via `input()`/`output()` — deeply nested presentational components don't reach into services directly, only container/page components do.

Flow: component calls a service method → service checks `PokemonCacheService` → cache hit returns the cached signal, cache miss triggers an HTTP call to PokeAPI, fills the cache, and updates the signal → component reads the signal (template or `computed()`).

**Services** (planned, under `core/services/`)
- `PokemonApiService` — sole access point for `/pokemon/{id|name}`, `/pokemon-species/{id}`, `/evolution-chain/{id}`, `/move/{id|name}` via `HttpClient`; returns data through `resource()`/`rxResource()` signals, no manual `.subscribe()`; always checks the cache first
- `PokemonCacheService` — in-memory `signal(new Map<string, Pokemon>())`, keyed by normalized lowercase name/ID, `get`/`set`/`has`; session-only (no `localStorage`); exists because PokeAPI's fair-use policy asks clients to cache rather than re-fetch
- `SearchIndexService` — powers global search; on first use loads a lightweight list of all Pokémon (`id`, `name_en`, `name_de` only, from `/pokemon-species/{id}` → `names` filtered by language) once for the app's lifetime, stored via `PokemonCacheService`/`signal()`; exposes a `computed()` that filters the index against the current search input (EN name, DE name, or numeric ID) for `GlobalSearchBar`'s live suggestions
- `MoveApiService` — separate service for `/move/{id|name}`, same caching strategy; used by `MovesetTable`'s hover tooltip (loads on demand, not upfront for the whole moveset) and `MoveDetailAccordion`'s full detail view

**Caching**: two levels — the per-entity in-memory signal cache (`PokemonCacheService`), and the one-time bulk-loaded search index (`SearchIndexService`, built on first `GlobalSearchBar` use, then filtered purely locally). No re-fetch for anything already cached; revisiting a Pokémon triggers no new HTTP call.

**Lazy loading**
- Overview grid: loads one page at a time via PokeAPI `limit`/`offset` (e.g. 20 at a time); `LoadMoreTrigger`'s `IntersectionObserver` calls `pokemonApiService.loadNextPage()`, results are appended (`update()`), not replaced
- Detail page: on open, fetches only `/pokemon/{id}` + `/pokemon-species/{id}`; `EvolutionChain` data loads only once that section actually renders (Angular `@defer`); individual move details load only on tooltip hover / detail-page click, never upfront
- `@defer` blocks for below-the-fold sections (`MovesetTable`, `EvolutionChain`)

**State principles**: no global store, services-with-signals are the single source of truth per data type; `computed()` for derived state (filtered moveset, search matches); `effect()` only for side effects (e.g. URL sync on filter change), never for computing state.

## Build order (see `claude md's/pokedex-roadmap.md`)

Ordered by technical dependency, not feature importance:

0. ✅ Tooling setup — install/configure Tailwind v4 (`@theme`, replace the current SCSS files), install Swiper.js, create `core/services`, `core/models`, `shared/components`, `features/*` folders, add data models
1. ✅ Core services only, no UI — `PokemonCacheService` → `PokemonApiService` → `MoveApiService` (`SearchIndexService` is deferred to step 4, it needs the fetching pattern proven first)
2. ✅ Routing skeleton for all three routes + `AppHeader` shell (`GlobalSearchBar` as an empty placeholder for now)
3. ✅ Overview page as the first vertical slice (`PokemonGrid`/`PokemonCard`/`LoadMoreTrigger`/`GenerationSelector`/`SearchFilterBar`) — proves the data layer end-to-end
4. ✅ Global search (`SearchIndexService` + finished `GlobalSearchBar`). Search works by ID, English name, and German name, and the same matching is shared with the overview's in-page `SearchFilterBar` name filter.
5. ✅ Pokémon detail page: `PokemonHeader` → `BaseStats` → `AbilitiesList` → `TypeEffectiveness` → `EvolutionChain` → `MovesetTable`/`MoveTooltip`
6. ⬅️ **next up** — Move detail page (`MoveDetailAccordion`)
7. Styling/responsive polish pass + loading/error/empty states for all `resource()` calls (not yet specified elsewhere)
8. Unit tests for the services/logic layer (cache, `SearchIndexService` filter, moveset filter `computed()`s)
